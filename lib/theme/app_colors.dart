import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  //==========================================================================
  // Background
  //==========================================================================

  /// 游戏主背景（草地）
  static const background = Color(0xFFE8F3D6);

  /// 窗口背景（羊皮纸）
  static const paper = Color(0xFFF8F1E7);

  /// 卡片 / 面板背景
  static const panel = Color(0xFFFFFBF5);

  //==========================================================================
  // Wood
  //==========================================================================

  /// 木头
  static const wood = Color(0xFFC89A63);

  /// 木头高光
  static const woodLight = Color(0xFFD9C2A3);

  /// 深木色（按钮、标题栏）
  static const tree = Color(0xFF5D4037);

  //==========================================================================
  // Nature
  //==========================================================================

  /// 草叶绿色
  static const leaf = Color(0xFF7CB342);

  /// 土壤
  static const soil = Color(0xFFA1887F);

  //==========================================================================
  // UI
  //==========================================================================

  /// 默认边框
  static const border = Color(0xFF3A2B20);

  /// 普通格子边框
  static const slotBorder = Color(0xFF8A735A);

  /// 当前选中（金黄色）
  static const selected = Color(0xFFE8B64A);

  /// Hover（桌面端）
  static const hover = Color(0xFFFFE7A5);

  /// 禁用
  static const disabled = Color(0xFFD3C7B5);

  //==========================================================================
  // State
  //==========================================================================

  /// 成功
  static const success = Color(0xFF81C784);

  /// 警告
  static const warning = Color(0xFFFFC857);

  /// 危险
  static const danger = Color(0xFFE57373);

  /// 信息
  static const info = Color(0xFF64B5F6);

  //==========================================================================
  // Text
  //==========================================================================

  static const black = Color(0xFF1D1D1D);

  static const white = Colors.white;

  static const textPrimary = Color(0xFF2F2A24);

  static const textSecondary = Color(0xFF6B6258);

  static const textHint = Color(0xFF9B9388);
}